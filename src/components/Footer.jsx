const Footer = () => {
    return (
        <footer>
            <p>
                &copy; {new Date().getFullYear()} Marie's Shop. Alle Rechte
                vorbehalten.
            </p>
        </footer>
    );
};

export default Footer;
